$ /opt/pyvenv/bin/python metrics.py --help
usage: metrics.py [-h] --real REAL --generated GENERATED -o OUTPUT
                  [--device DEVICE] [--batch-size BATCH_SIZE]

Optional torch-fidelity FID/IS. Not the original TensorFlow score-SDE
protocol. Install: uv sync --extra metrics. Inception weights download on
first use. MPS-trained models can be evaluated here on CPU (or a separate CUDA
machine).

options:
  -h, --help            show this help message and exit
  --real REAL
  --generated GENERATED
  -o, --output OUTPUT
  --device DEVICE       cpu or cuda; Inception evaluation is not on MPS
  --batch-size BATCH_SIZE
